module.exports = {
  testEnvironment: "node",
  testMatch: ["**/*.spec.js"],
};

// const config = require("./jest.config");
// config.testMatch = ["**/*.spec.js"];
// module.exports = config;

// package.json "test:unit": "npm test -- --watch -c jest-unit-config.js",
